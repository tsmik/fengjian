# 人相兵法 — 規則引擎規格
> 目標：將觀察問題與評分規則從程式碼抽離為資料，實現「改標準不改程式」
> 版本：v1.3 | 建立日期：2026-03-25 | 最後更新：2026-03-27 | 狀態：已完成

---

## 一、現狀

觀察問題和評分規則已完全抽離為 JSON 資料，存在 Firebase，透過 admin.html 管理介面編輯。程式本身（規則引擎 + UI 框架）不需要隨標準變動而修改。

condResults（條件顯示資訊）由 `_buildPartCond()` 從 JSON 規則樹自動產生，不再手動維護。

---

## 二、架構

```
觀察問題 JSON ──→ 產生問卷介面 ──→ 學員填答 ──→ obsData
                                                    ↓
評分規則 JSON ──→ 規則引擎讀取 ──→ 比對 obsData ──→ 13維度結果
                                                    ↓
                                              condResults（自動產生）
                                                    ↓
                                              13維度觀察評分頁（顯示）
```

---

## 三、觀察問題 JSON 結構

（與 v1.2 相同，略）

---

## 四、評分規則 JSON 結構

（與 v1.2 相同，略）

---

## 五、引擎處理流程

（與 v1.2 相同，略）

---

## 六、管理介面

admin.html 提供圖形化編輯器，支援評分規則和觀察問題的編輯。詳見「線一開發筆記」中的 admin.html 章節。

---

## 七、遷移步驟（全部完成）

1. ✅ 建立規則引擎（通用計算函數）— rule_engine.js 完成
2. ✅ 把現有 13 個維度的寫死邏輯轉為 JSON 規則 — 65 cases 全 PASS
3. ✅ 驗證新舊引擎結果一致
4. ✅ 切換到新引擎（整合進 index_desktop.html）
5. ✅ 建立管理介面（admin.html）
6. ✅ 觀察問題編輯器 + Firebase 讀取
7. ✅ 移除舊邏輯（7a 移除 data[][] 賦值行和 TH 系統，淨減 441 行；7b condResults 改由引擎自動產生，淨減 1005 行）

---

## 八、運算元總覽

| 運算元 | 功能 | 結構 | admin 顯示 |
|--------|------|------|-----------|
| 比對條件 | 觀察答案 = match → true | `{ ref, match, side? }` | `h1 頂骨型相 = 龜背` |
| `AND` | items 全部 true → true | `{ op: "AND", items: [...] }` | 都符合 AND |
| `OR` | items 任一 true → true | `{ op: "OR", items: [...] }` | 或 OR |
| `COUNT` | items 中 true 的加權總數 ≥ min → true | `{ op: "COUNT", min: N, items: [...] }` | 計數 COUNT 最少N項 |
| `LR` | 左右各自判定後匯總 | `{ op: "LR", merge: "any"/"all", each: {...} }` | 左右部位 LR |
| `VETO` | 觸發條件 → 強制判定結果 | `{ veto: [{condition, result}], rule: {...} }` | VETO |
| `NOT` | 條件結果取反 | `{ op: "NOT", item: {...} }` | 不可有 NOT |
| `partResult` | 引用同維度其他部位的結果 | `{ partResult: "部位" / "部位.L" / "部位.R" }` | 引用結果 → 左眉 |
| `group` | COUNT 內的顯示分組（v1.3 新增） | `{ group: "敘述文字", items: [...] }` | 敘述分組 |

所有運算元支援任意巢狀組合。

### group 節點詳細說明（v1.3 新增）

**結構：**
```json
{
  "group": "頂骨要圓",
  "items": [
    { "ref": "h1", "match": "龜背", "side": "L" },
    { "ref": "h1", "match": "龜背", "side": "R" }
  ]
}
```

**用途：** 在 COUNT 的 items 裡把多個條件分組，並加上老師的敘述文字。學員在 13 維度觀察評分頁看到的是摺疊式的分組，點擊敘述展開看具體條件。

**計分邏輯：** group 不參與計分。evaluate 引擎遇到 group 節點時穿透到裡面的 items，每個 item 各自獨立計分（各自有自己的 weight）。

**限制：**
- group 只能出現在 COUNT 的直接子層
- 不能巢狀 group
- group 和非 group items 可以混合存在
- group 是可選的，沒有 group 的規則完全向下相容

**condResults 處理：** group 內的 items 展開後，每個 condResult item 帶 `groupLabel` 欄位。cpRenderMain 按 groupLabel 分組顯示。

**admin.html 支援：** 尚未實作。目前需手動在 Firebase console 編輯 group 結構。

### NOT 運算元詳細說明（v1.2 新增）

**結構：**
```json
{
  "op": "NOT",
  "item": { "ref": "n5", "match": "無起節" }
}
```

**邏輯：** 遞迴 evaluate item，然後取反（true → false，false → true）。

**注意事項：**
- NOT 只包一個 item（不是 items 陣列）
- item 可以是任何節點類型
- NOT 的結果是布林，不參與 COUNT 的加權計算

---

## 九、DIMS 與 DIM_RULES 的對應關係

DIMS 陣列定義每個維度的顯示名稱（a/b）和動靜類型（aT/bT）。DIM_RULES 定義 positive/negative。

**關鍵規則：** `DIMS[i].a` 必須等於 `DIM_RULES[i].positive`，`DIMS[i].aT` 必須等於 `DIM_RULES[i].positiveType`。因為 evaluate 引擎算出的 `A` 代表 positive，DIMS 渲染時用 `A` 對應 `a`。

| dimIndex | DIMS a (=A) | DIMS b (=B) | aT | bT |
|----------|-------------|-------------|-----|-----|
| 0 | 形 | 勢 | 靜 | 動 |
| 1 | 經 | 緯 | 靜 | 動 |
| 2 | 圓 | 方 | 動 | 靜 |
| 3 | 直 | 曲 | 靜 | 動 |
| 4 | 收 | 放 | 靜 | 動 |
| 5 | 緩 | 急 | 靜 | 動 |
| 6 | 順 | 逆 | 靜 | 動 |
| 7 | 合 | 分 | 靜 | 動 |
| 8 | 真 | 假 | 靜 | 動 |
| 9 | 攻 | 守 | 動 | 靜 |
| 10 | 奇 | 正 | 動 | 靜 |
| 11 | 實 | 虛 | 靜 | 動 |
| 12 | 進 | 退 | 動 | 靜 |

---

*版本：v1.3 | 更新日期：2026-03-27*
*v1.0 建立：初始規格*
*v1.1 更新：COUNT weight 欄位*
*v1.2 更新：NOT 運算元；admin.html 功能擴充*
*v1.3 更新：group 敘述分組節點；DIMS/DIM_RULES 對應關係表；遷移步驟全部完成*
