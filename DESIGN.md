# DESIGN.md — 人相兵法設計系統
> CC 每個 session 開始時讀此檔。只能使用此處定義的 token，不准臨時發明數值。

<!--
來源：design-decisions-raw.md（178 則設計回饋，2026-05-23 ~ 2026-06-06）。
收斂方式：同維度前後矛盾以「時間最新」為準；[×N] = 該規則在原始回饋出現的概略次數
（N 大＝已成慣例的鐵則；N=1~2＝待確認）。解不掉的列入「待你裁決」，不臆測。
註：raw 檔含 context 壓縮摘要與貼入的開發筆記文件（非 Mike 親打回饋），已排除。
-->

## Design Tokens

### 間距尺度（spacing）
- `--gap-tight` 半個中文字寬（≈0.5em）— 主次微距，如「先天/運氣/後天」相對「老闆/主管」往左推半字以分主次
- `--gap-1` 一個中文字寬（≈1em）— 並排元素的最小可視間隔（Mike：「間隔一兩個全形空格之類的概念」）
- `--sticky-gap` 28px — sticky 欄頂端留白（**必須不透明**，見禁止區）
- `--tab-page-pad` 20px 16px — 分頁外距
- 行距 `--lh-card` 1.12（個案卡片）／`--lh-note` 1.55（群組名／側欄多行）
- **原則**：同一畫面內，筆記↔橫線↔下一區塊的間距要等距；嚴禁「有些鬆有些緊」

### 色票（color）
底色系（米）
- `--bg` `#f7f4ef` 頁面底
- `--cream-bg` `#f0e9dc` / `#f3eee4` 區塊米底
- `--cream-deep` `#cdb892` 深米（墊底／參考線）
- `--cream-text` `#7d6643` 米系文字
- `--text` `#3a3228` 深米色內文（**取代黑色**）

動靜訊號（唯二強調色，只用於動靜／評分／條件）
- `--active` `#C17A5A` 動／勢（橘）
- `--static` `#7A9E7E` 靜／形（綠）

群組色（報告圖 + 群組細色條，取自報告頁）
- `--grp-boss`／先天 `#936A78`（老闆同色）
- `--grp-mgr` 填色 `#AE6D4F`／文字 `#876D4F`（主管）
- `--grp-luck` `#546D77`（運氣）
- `--grp-post` `#797181`（後天）
- `--sector-pre` `#854F51`（先天扇形／文字；coef 變體 `#8E4B50`）
- `--total` `#494541`（總係數＝RGB 73,69,65；字白）
- 深米分隔線 `#898179` / `#8a7440` / `#b09a6a`

不透明度（報告圖既定）
- 扇形 0.3｜先天 0.3｜老闆/主管輪廓 0.5｜bar 低分 0.5 / 高分 0.7｜平衡線白 0.5
- 個案染色（case tint）**0.12**（標題列同色但濃一階 0.20）— 詳見待裁決 #2

### 字級（type scale）
- `--fs-body` **14px** — 筆記內文＝全站內文基準。**姓名以外的字一律比照此值**（個案卡、群組名、hint 等）[×3]
- 姓名／頁標題：比照「形勢」字 **22px**（含字距，不粗體）— 如「個案管理」「形勢 看 格局」
- 維度名／被選維度標頭：比照「符合條件為形」說明字（中型，深米色）
- **圖表字級**（逐圖微調，見待裁決 #4）
  - 手機 radar2_m：維度 15.5｜數字 14｜先天/運氣/後天框 12｜總係數 scale 1.3
  - 手機 radar3：維度 14｜係數數字 13.5｜動/靜極字 13.5｜中央先天運氣後天 12.5
  - 通則：同一張圖內維度字與數字儘量等大；標題字＝維度字大小；兩張並排圖維度字與標題字互相一致
- **嚴禁字級種類過多**（Mike 反覆：「字級分布有點多 看起來有點亂」）[×3]

### 圓角與陰影（radius / shadow）
- `--r-box` 9px — 卡片／子卡（如 `.m-sv-subtitle` 上緣 `9px 9px 0 0`）
- `--r-pill` 16px — tab／膠囊按鈕
- `--r-icon` 圓形 — icon 外圈（鉛筆／叉叉／i／橡皮擦皆「符號外加一圈」）
- 動靜 bar：右側圓角（子彈圖）或兩側圓角（動靜總覽）
- 陰影：Mike 從未要求投影；維持極簡、以米底色塊與細線分層，不靠 shadow

## 硬規則（Rules）
<!-- 每條附「出現次數」。例：[×12] 內文字級統一 14px，姓名為唯一例外 -->

- [×8] **不要粗體**。任何文字預設 regular weight；Mike 反覆把粗體拿掉（標題、維度名、選取項、卡片摘要皆然）
- [×3] **不要黑色，用深米色 `#3a3228`** 取代所有黑字
- [×3] **內文一律 14px**（＝筆記內文）；唯一例外是姓名／頁標題（22px）
- [×10+] **動＝橘 `#C17A5A`、靜＝綠 `#7A9E7E`**，且這兩色只給動靜／評分／條件訊號用，不濫用於裝飾
- [×25+] **對齊是第一優先**：邊緣對齊（左緣／右緣）、頂對齊、框上緣對齊；數字串寬度壓到等於對應中文字寬度（不是把字撐大）
- [×3] **字級種類要少**：同畫面避免多種大小並陳；新元素用「比照既有元素」決定大小，不另發明
- [×15+] **比照既有、保持一致**：新頁面的側邊欄寬度／按鈕大小／選取呈現／字體一律「比照」既有頁（基準＝上課的「自我評分」頁）
- [×5] **sticky 提示**：被選的維度名／部位名要固定在欄頂，下捲時不滑走（學員要隨時知道在填哪個）
- [×5] **省版面**：能省則省，去掉冗字（如「條件」二字、逐條說明列、重複的評斷標準復述）
- [×3] **刪除必須二次確認**：跳警示「確定要刪除…嗎」，且警示要帶入被刪內容、不要出現網域那行字
- [×3] **空輸入框點外面就收起**，不留空框（筆記／新增條件皆然）
- [×2] **圖表用長度不用面積**：面積會失真（動靜面積看起來差太多）；不用玫瑰圖
- [×2] **染色（個案色）要在底層**，只當背景，不可整層蓋住所有元素
- [×2] **雷達圖無數值時要有米底色**，不可透明

## 專案慣例（Conventions）
<!-- 已成型的具體做法 -->

- **編輯入口＝鉛筆圈圈**（pencil-in-circle）[×7]：取代 emoji ✏️、也取代「點此編輯」之類文字標籤；按下後**不變色（不變紅）**；icon SVG 約 14px
- **筆記＝小黃便利貼概念**：黃底、字色配合便利貼；點鉛筆圈圈展開 `textarea rows="1"`「寫多少顯多少」、即打即存、原生 undo、可下拉拉高；刪除用**橡皮擦 icon**放在鉛筆圈圈右邊
- **+條件按鈕**：圓角框、風格同鉛筆圈圈；字走藍灰色系、配淡藍底；放在部位名旁邊（不要堆在最後一條下方）
- **i 圈圈＝hint**：hint 不要自己的外框／底色，沿用筆記的字體/大小/顏色，只是 icon 換成 i
- **icon 通則**：符號外加一圈、不上色、不加底色、不搶視覺、不佔版面
- **強調標籤＝色底白字**：如「係數＝動」用該維度動/靜色當底、白字（比純文字清楚）；「加總」之類直接落在底色上、不另給白框
- **卡片**：寬度寫死，超寬內容尾字用「…」；不依男女分色；預設米色，顏色可由色卡選（用圓形色點標示，不用下拉選單）
- **動靜 bar**：數字落在 bar 內、不貼上下緣、不粗體；值為 0 的那側不標 0（只標另一側）；需附動/靜圖例
- **分享/匯出三選項**：分享表格報告／分享圖表／分享表格報告＋圖表（橫排）
- **群組排序**：用上下箭頭，不用拖拉
- **字體**：標題與正文用霞鶩文楷（LXGW WenKai Screen）+ Noto Sans TC fallback；**圖表內繞排小字改用 sans**（WenKai 小字會糊）
- **RWD**：`m.html`（手機）＋ `app.html`（桌機）共用 `m_*.js`，兩者只差桌機 `@media` 區塊；先調桌機、手機後跟，「不要一次調兩個」
- **流程紀律**：一律先 staging 測 → Mike 點頭才上 main；桌機舊報告 `index_desktop.html` / `manual.js` 是學員老師重視的內容，**完整保留、不要動**

## 禁止（AI 典型敗筆）
<!-- 在這個專案會被 Mike 退件的做法 -->

- ❌ **粗體**當強調手段（一律 regular）
- ❌ **黑色文字**（用深米色）
- ❌ **字級種類過多**、大小分布雜亂
- ❌ **間距鬆緊不一**、行距不協調
- ❌ **透明造成穿透/看不見**：sticky 頂端留白透明導致內容透過去（很醜）、雷達無值透明、預覽白底使白色按鈕看不見 — 一律給不透明底
- ❌ **染色蓋在最上層**罩住全部元素（應在底層當背景）
- ❌ **emoji 當 icon**（用鉛筆圈圈那套線性 icon）
- ❌ **預設就攤開編輯框**（應先顯示鉛筆圈圈，點了才進編輯）
- ❌ **玫瑰圖／用面積表達數值**（失真，用長度）
- ❌ **預留過寬的按鈕留白**（維度按鈕左右太寬、部位與維度名距離太寬）
- ❌ 紫色漸層、無腦全置中、shadcn 預設原封不動 — 本專案是米色＋霞鶩文楷的東方調性，預設樣式一律收斂進上述 token

## 待你裁決（Unresolved）
<!-- CC 解不掉的矛盾與模糊處，列在這給 CEO 決定 -->

1. **選取狀態顏色不統一**：維度框／部位框的「被選取」Mike 明確要求統一成**淡米底**（取代淡綠底＋紅邊線）；但題目選項被選取目前仍是**淡綠色底**。這是刻意分層（區塊選取 vs 選項選取），還是該全部統一？未見明確指示。

2. **個案染色濃度最終值**：序列為 0.15 → 0.11 → 0.12。最新一則（2026-06-06 02:46）是 **0.12**，我依「最新為準」取 0.12（標題列濃一階 0.20）；但同日稍早（01:53）才說 0.11，兩值很近、時間很接。請確認定 0.12 還是 0.11。

3. **維度是否用 13 色**：報告圖要「13 維度顏色＋米色」（顏色越多越好辨識）；但自我評分鷹架的維度欄要「不要 13 色、改深米色」。我判定為**不同情境各自適用**（圖＝13色、表單欄＝深米），非矛盾。若你要全站統一一種做法，請裁示。

4. **圖表字級無單一 token**：各圖、各平台給過很多組數值（radar2_m 維度15.5/數字14；radar3 維度14/數字13.5/極字13.5/中央12.5；桌機又另有調框結果）。目前只能訂「同圖內維度字≈數字、標題＝維度字大小」的相對原則，實際 px 以各圖最新調框值為準。要不要我替你固定一份各圖的絕對字級表？

5. **要不要一份全站絕對字級階層表**：你多次說「字級太多很亂」要收斂，但從沒給出明確的階層表，全靠「比照某元素」相對指定。是否由我訂一份 3–4 級的絕對字級表（如 22 / 15 / 14 / 13）供全站套用，取代到處「比照」？這能根治「字級分布雜亂」，但會動到既有逐圖微調值，需你點頭。
