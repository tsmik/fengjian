# P2 接線盤點（P1 現用 vs P2 新建）

> 2026-06-25 盤點。目的：接線前把「現在實際在用的 P1」與「新建的 P2」整套技術棧攤開對照，定位接線真正要做什麼、風險在哪。

> **命名（Mike 2026-06-25 定）**：`P1`＝現用正式站（`renxiangbingfa`，沿用程式碼 import_p1/rbf1 命名）；`P2`＝新站（`rbf2app-staging`）。不再用「P0」。
>
> **🔒 硬約束**：P1 日後完全照現在方式運作；**P2 絕不碰 P1**——不同 Firebase 專案、不同網址、不共改 `obs_recalc.js`/`rule_engine.js`/`settings/*`。P2 學員前台是獨立副本。

---

## 一、三層現況一覽

| | **P1（現在實際在用）** | **P2（新後台，建置中）** |
|---|---|---|
| Firebase 專案 | `renxiangbingfa`（正式）/ `renxiangbingfa-staging`（測試） | `rbf2app-staging` |
| 網址 | `tsmik.github.io/fengjian/`、`fengjian.pages.dev` | `rbf2app-staging.web.app/p2/`（只有後台，**無學員前台**） |
| 桌機學員入口 | `index_desktop.html` / `index.html` / `app.html` → `js/app.js` | （尚未有） |
| 手機學員入口 | `m.html` → `js/m_main.js` → `js/m_input.js` | （尚未有） |
| 規則/題庫後台 | `admin.html`（舊） | `p2/admin2.html`、`observations.html`、`rulesets.html`（新） |
| 規則資料 | `settings/rules`（rulesJson＝13維陣列，舊節點文法 AND/OR/COUNT/LR） | `ruleSets/{id}/dims/{0..12}`（新 DNF：部位→卡片→combo→葉） |
| 題庫資料 | `settings/questions`（questionsJson） | `ruleSets/{id}/observations/{qid}` + `obsmeta/layout` |
| 上線指標 | （單一 live） | `config/active` → `{activeRuleSetId, defaultSpice}` |
| 計分引擎 | `js/rule_engine.js`（舊） | `p2/js/engine.js` ＋ `js/rule_engine_v2.js`（兩個，見下） |

**關鍵事實**：P1 桌機與手機**共用** `js/obs_recalc.js` → `js/rule_engine.js`（舊）＋共用 `settings/rules`＋`settings/questions`。動 obs_recalc／舊引擎會**同時影響現用桌機與手機**。

---

## 二、接線的真正意義（含 P1 後）

P1 學員前台（桌機 `index_desktop.html`+`app.js`、手機 `m.html`）現在整套跑在 **renxiangbingfa**，用舊引擎＋舊資料，由 **admin.html** 維護。

「接線」＝把這套學員前台**複製/改造成 P2 版**：指向 `rbf2app-staging`、讀新 `ruleSets` 格式、用新引擎。這是**跨 Firebase 專案**的工程，不是單純改幾行 import。

**建議策略**：平行新建 P2 學員前台（指向 rbf2app），**P1 renxiangbingfa 完全不動、繼續 live**。對齊 roadmap 第 5 步「學員前台部署到 rbf2app-staging 域名」。避免就地改 obs_recalc（它被現用桌機+手機共用，風險最高）。

---

## 三、引擎斷層（接線最關鍵的前置關卡）

有**兩個 v2 引擎，各做了一半**，沒有任何一個能正確算「admin2 現在存的資料 ＋ 辣度第1層」。

| 能力 | `js/rule_engine_v2.js`（前台現有） | `p2/js/engine.js`（後台，有 node 測試） |
|---|---|---|
| 辣度第1層（選項切點 `leaf.spice={選項:level}`） | ✅ `acceptedOptions()` | ❌ 只認 `leaf.match` |
| 左右由觀察題 `paired` 自動判 | ❌ 用舊 `lrMode`（admin2 已 `delete`，恆為 none） | ✅ `isPaired` 逐側 |
| poleFlip 動靜反轉 / `targetPole` | ❌ 沒有 | ✅ `dimDef.targetPole` |
| 吃 admin2 的 `{leaves:[]}` combo 格式 | ❌ 預期扁平陣列，會壞 | ✅ `comboLeaves()` 兩種都吃 |
| 聚合部位骨架 | 從資料讀 | ✅ `AGG_FIXED` 固定骨架（對齊設計） |
| 辣度第2層（部位 `spice={大/中/小辣:數量}`） | ✅ `resolveAuxThreshold` | ✅ `auxNeed` |

**證據**：
- `p2/js/admin2.js:169` 存檔時 `delete p.lrMode`、`delete p.targetPole`（「左右由觀察題 paired 決定」）。
- `p2/js/admin2.js:172` combos 包成 `{leaves: cb}`（Firestore 不接受巢狀陣列）。
- 所以 admin2 寫出的資料**只有 `p2/js/engine.js` 能正確吃**，但它缺辣度第1層。
- 離線三段驗證用的 `~/fengjian_audit/analysis/engine_v2_standalone.mjs` 是 `js/rule_engine_v2.js` 的複本——那次驗證的引擎其實**跟 admin2 真實輸出格式對不上**，已驗數字（靜 206→257→365）需重驗。

**結論**：接線前要先把兩者合成**一個正確的前台引擎**，再用 10 真實樣本離線重驗動靜/係數。**建議以 `p2/js/engine.js` 為底**（已對齊 admin2 真實輸出），把 `rule_engine_v2.js` 的「選項辣度第1層」那一小段搬進去——比反過來補的東西少、風險低。此步**完全在 node 離線跑、不碰任何前台**。

---

## 四、資料格式落差（接線要寫的轉換層）

學員前台目前吃的記憶體結構，與 P2 Firestore 文件格式不同，需轉換層：

1. **題庫** `ruleSets/{id}/observations/{qid}`（扁平：`{obsId, part, section, label, paired, options, optionHints}`）
   → 前台 `OBS_PARTS_DATA`（巢狀：`{部位:{sections:[{label, qs:[{id, text, paired, opts:[{v,hint}]}]}]}}`）。
   需依 `part`/`section` group + 欄位映射（`label→text`、`options[]→opts[{v,hint}]`、`optionHints` 展開），並依 `obsmeta/layout` 排序。
   - 來源：`p2/js/obs_editor.js:86`（toDoc）；目標：`js/core.js:41`（OBS_PARTS_DATA_DEFAULT）。

2. **規則** `ruleSets/{id}/dims/{di}`（`{dimIndex,dimName,positiveType,negativeType,targetPole,poleFlip, parts:{部位:{kind,cards:[{role,combos:[{leaves:[...]}]}], spice}}}`）
   → 若引擎以 `p2/js/engine.js` 為底，可**幾乎直接吃**（combos `{leaves}` 已支援）；只需確認辣度第1層搬入後 `leafMatch` 仍正確。

---

## 五、接線分步（建議順序，每步可獨立驗證）

1. ✅**（離線・零風險）合成單一前台引擎**：以 `p2/js/engine.js` 為底，搬入辣度第1層 `acceptedOptions`（`level` thread 到 `leafMatch`，向後相容）。驗證全過：既有 `engine_test.mjs`、辣度第1層機制單元、10 真實樣本重驗（新逐側引擎 290→333→420 單調✅；dim9/10/12 攻守/奇正/進退 遞減＝動極維，符合 Mike 先前確認）。
2. ✅**（新檔・不碰現用）資料轉換層**：`p2/js/ruleset_convert.js`（純函式：`convertObservations`/`collectDims`/`buildIsPaired`）+ `p2/js/load_ruleset.js`（firebase 包裝 `loadActiveRuleSet(db)`，預設辣度大辣）。單測 `ruleset_convert_test.mjs` 14/14 過。真資料 round-trip 待第 3 步瀏覽器直讀 rbf2app。
3. **平行新建 P2 學員前台**：複製桌機/手機入口，Firebase 指向 `rbf2app-staging`，接步驟 1 引擎 + 步驟 2 轉換層；obs_recalc 改呼叫新引擎（在 P2 副本內，不動 P1）。
4. **設 `config/active`**＝`set-20260610020325`（目前 404；admin2「設為上線」會寫）。
5. **部署 P2 前台**到 rbf2app 域名；Mike 用他的學員帳號（tw.michaeltseng）登入實測跑通。
6. 板書/講義讀取一併接（lectures 內容 Mike 另補）。

> 驗證紀律：admin2/前台需登入無法瀏覽器自動測 → `node --check` ＋ 離線引擎跑真實資料 ＋ Mike staging 實測；改核心分小步、每步推 staging。

---

## 六、學員端辣度選擇（Mike 2026-06-25 定）

**現況**：前台**完全沒有辣度選擇器**（辣度是 P2 才有；`config/active.defaultSpice` 目前只有後台寫、前台沒人讀）。設計意圖見 `p2/js/admin2.js:679`：「學員看報告時選大/中/小辣」。

**決策＝報告頁即時切換、記住每人選擇**：
- 報告頁放「大/中/小辣」三選一選擇器；初始預設＝`config/active.defaultSpice`（預設＝大辣，Mike 2026-06-25 定）。
- 學員選了就**記住（存在他自己的帳號）**，之後計算＋存檔都用這個辣度。
- ✅admin2/rs_manager 預設已改 `defaultSpice:'大辣'`（`admin2.js:815`、`rs_manager.js:181/192`，2026-06-25），接線時要一併改成 `'大辣'`。
- 切換辣度→用該 level 重算 13 維動靜/係數→重繪報告＋更新存檔（dataJson 反映所選辣度）。
- 聚合部位門檻固定、不受辣度（`AGG_FIXED`）。
- 持久化位置：學員帳號層級（per-student，非 per-case），實作時定欄位（與 obsJson/manualDataJson 同一份 user 資料）。

---

## 附：repo 內各 html 對應 Firebase 專案

- `renxiangbingfa`（P1 正式）：`index_desktop.html`、`index.html`、`app.html`、`m.html`、`admin.html`、`photo_training.html`、`p2/import_p1.html`(P1端)
- `renxiangbingfa-staging`（P1 測試）：上列 host 為 `staging.fengjian.pages.dev` 時自動切換
- `rbf2app-staging`（P2）：`p2/admin2.html`、`observations.html`、`rulesets.html`、`lectures.html`、`liunian.html`、`users.html`、`import_*.html`、`compare_rbf1_rbf2.html`
